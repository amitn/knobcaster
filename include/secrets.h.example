// Copy to include/secrets.h (gitignored) and fill in your Wi-Fi credentials.
// For MVP bring-up these are compiled in; later we move to BLE/portal provisioning.
#pragma once

#define WIFI_SSID  "your-ssid"
#define WIFI_PASS  "your-password"
